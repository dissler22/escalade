# Command Templates

This directory is intentionally present even when no command-specific templates
are defined yet.

Keep at least one Markdown file here so tooling that scans
`.specify/templates/commands/*.md` does not fail on a missing path.
